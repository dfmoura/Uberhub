#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<string> myVector = {"apple", "banana", "orange"};
    int lastIndex = myVector.size() - 1;
    string lastString = myVector[lastIndex];
    cout << lastString << endl;
    return 0;
}